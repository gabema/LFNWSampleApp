<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="gabema.lfnwsampleapp"
    android:versionCode="1"
    android:versionName="1.0" >

    <uses-sdk
        android:minSdkVersion="7"
        android:targetSdkVersion="17" />

    <application
        android:allowBackup="true"
        android:icon="@drawable/ic_launcher"
        android:label="@string/app_name"
        android:theme="@style/AppTheme" >
        <activity
            android:name="gabema.lfnwsampleapp.MainActivity"
            android:label="@string/app_name" >
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>

        <provider
            android:name="gabema.lfnwsampleapp.SampleWebProvider"
            android:authorities="swp"
            android:enabled="true"
            android:exported="true" >
        </provider>
        <provider
            android:name="gabema.lfnwsampleapp.SimpleProvider"
            android:authorities="lfnwsimple"
            android:enabled="true"
            android:exported="true" >
        </provider>
    </application>

</manifest>